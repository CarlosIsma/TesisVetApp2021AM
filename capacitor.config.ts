import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.veterinarias.tesis',
  appName: 'veterinariasCO',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
